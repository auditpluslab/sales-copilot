import { chatCompletion } from "./client"
import type { Insight, DeepDiveQuestion, SuggestionCard } from "@/types"

export async function generateSuggestions(
  insight: Insight,
  transcriptText: string
): Promise<{
  questions: DeepDiveQuestion[]
  proposals: SuggestionCard[]
}> {
  // システムプロンプト：Chain of Thoughtを強制
  const SYSTEM_PROMPT = `あなたは最高の営業コンサルタントです。
クライアントとの会話から、**その場でしか生まれない質問と提案**を生成してください。

## 【絶対ルール】
1. **必ず思考プロセスを出力してください** - これをスキップして直接JSONを出力することは絶対に禁止です
2. **各ステップを丁寧に埋めてください** - 「特になし」「省略」のような空欄は禁止です
3. **テンプレート化された提案は絶対に禁止です**
4. **安全で一般的な回答は禁止です** - 具体的な文脈から外れた回答は避けてください

## 禁止ワード（絶対に使わないでください）
- 「テンプレート」「ダッシュボード」「可視化」「ワークショップ」
- 「PoC」「システム導入」「ソリューション」「DX推進」「デジタル変革」
- 「最適化」「効率化」「自動化」「プラットフォーム」
- 「導入」「導入効果」「導入計画」「段階的に」「包括的」

## 【良い質問の例】
❌ 「導入の目的と期待される効果について具体的にお聞かせいただけますか？」
✅ 「月20件の見積もり作成に40時間かかっているとのことですが、その2時間の内訳で特に時間がかかる部分は、商品選定・単価確認、書式作成、計算ミスのチェックのどれでしょうか？」

❌ 「予算についての制約や要件はありますか？」
✅ 「年間300万円の予算枠とのことですが、これは見積もり作成の効率化のみに充てる予算でしょうか、それとも他の業務改善も含めて検討されているでしょうか？」

## 【良い提案の例】
❌ 「導入効果の試算：現在5件の発言があります。貴社の現状から想定される効果を試算します。」
✅ 「Excelの見積もりテンプレートにVBAマクロを追加して、商品選定をドロップダウンから選べるようにします。これにより、見積もり作成時間が1件あたり2時間から30分に短縮され、月20件であれば40時間から10時間に、年間で360時間の削減が見込めます。」

❌ 「導入プランの提案：段階的な導入アプローチをご提案します。まずは小規模なパイロットから始め、効果を確認しながら拡大します。」
✅ 「まずは1つの見積もりテンプレートだけ修正して、1週間試してみませんか？実際に使ってみて問題があれば、その都度調整します。1週間後、効果が確認できたら他のテンプレートにも展開しましょう。」

## 思考プロセス（必ず出力してください）
提案を作る前に、必ず以下の5ステップの思考プロセスを出力してください。

### ステップ1: 会話から情報の完全抽出
会話の中から以下を全て抽出してください。空欄は禁止です。
- 具体的な数字（時間、回数、金額、人数、割合）：
- 具体的なツール名（Excel、Chatwork、Slack、Zoom、 Salesforceなど）：
- 具体的な人物（役割、担当者）：
- 具体的なシーン（いつ、どこで、どんな状況で）：
- クライアントが発した**具体的な言葉**（そのまま引用）：

### ステップ2: 課題の具体的定義
抽出した情報から、課題を具体的に定義してください。
- **誰が**：
- **いつ**：
- **どんな状況で**：
- **何に困っているのか**（具体的な数字と感情を含める）：

### ステップ3: 解決策の具体化（3条件）
課題に対して、以下の3条件を全て満たす解決策を考えてください。
- **条件1（10分で導入）**：
- **条件2（既存ツール活用）**：
- **条件3（具体的な効果）**：

### ステップ4: 抽象語を具体語に変換
抽象的な言葉を具体的なアクションに変換してください。
- 「テンプレート化」→「よく使う3つの商品をExcelのドロップダウンリストに登録」
- 「可視化」→「Chatworkに件名と担当者を投稿する」
- 「効率化」→「1件あたり30分短縮」
- 「自動化」→「Googleフォームと連携してSpreadsheetに自動蓄積」

### ステップ5: 提案のセルフチェック
提案を作る前に、以下のチェックを通してください。
1. クライアントの言葉をそのまま引用しているか？：
2. 具体的な数字が含まれているか？：
3. 具体的なツール名が含まれているか？：
4. 10分で導入できるか？：
5. 既存ツールの活用か？：
6. 具体的な効果（数字）が示せているか？：

## 出力形式
1. **まず**、上記5ステップの思考プロセスを詳しく出力してください
2. **その後**、JSON形式で出力してください（質問は最大3つ、提案は最大2つ）

重要：思考プロセスを飛ばしてJSONだけを出力しないでください。必ず思考プロセスから始めてください。`

  // ユーザープロンプト：会話データと思考プロセスの強制
  const userPrompt = `以下の会話から、質問と提案を生成してください。

## 【絶対ルール】
1. **必ず思考プロセスから出力を開始してください**
2. **思考プロセスを飛ばしてJSONだけを出力することは絶対に禁止です**
3. **各ステップを具体的に埋めてください** - 「特になし」は禁止です

## 会話内容
${transcriptText}

## インサイト
${JSON.stringify(insight, null, 2)}

---

## 出力開始：思考プロセス

以下の形式に従って、思考プロセスから出力を開始してください。

**【ステップ1: 会話から情報の完全抽出】**
- 具体的な数字：
- 具体的なツール名：
- 具体的な人物：
- 具体的なシーン：
- クライアントが発した具体的な言葉：

**【ステップ2: 課題の具体的定義】**
- 誰が：
- いつ：
- どんな状況で：
- 何に困っているのか：

**【ステップ3: 解決策の具体化（3条件チェック）】**
- 条件1（10分で導入できるか）：
- 条件2（既存ツールを活用するか）：
- 条件3（具体的な効果が示せるか）：

**【ステップ4: 抽象語を具体語に変換】**
- 変換が必要な抽象語と具体語のペア：

**【ステップ5: 提案のセルフチェック】**
1. クライアントの言葉をそのまま引用しているか？：
2. 具体的な数字が含まれているか？：
3. 具体的なツール名が含まれているか？：
4. 10分で導入できるか？：
5. 既存ツールの活用か？：
6. 具体的な効果（数字）が示せているか？：

---

## 出力終了：JSON

思考プロセスを出力した後、以下のJSONを出力してください。

\`\`\`json
{
  "questions": [
    {
      "question": "質問文（クライアントの言葉を引用し、数字や具体名を含める）",
      "intent": "なぜこの質問を聞くのか",
      "category": "constraint | value | timeline | budget | decision | competition",
      "priority": 1〜3,
      "evidence": "この質問の根拠となる具体的な発話をそのまま引用"
    }
  ],
  "proposals": [
    {
      "title": "具体的な提案タイトル（クライアントの言葉を含める）",
      "body": "提案の詳細説明（数字、具体名、クライアントの言葉を必ず含める）",
      "reason": "なぜ今この提案が自然か（クライアントの発言を引用して説明）",
      "expected_value": "期待される効果（数字で示す）",
      "next_step_phrase": "次の一言（「まずは〇〇から始めませんか？」形式）",
      "evidence": "根拠となる発話をそのまま引用",
      "confidence": "high | medium | low",
      "rank": 1〜2
    }
  ]
}
\`\`\`

重要：思考プロセスから出力を開始してください。JSONだけを出力しないでください。`;

  // 会話が十分でない場合は空のレスポンスを返す
  if (transcriptText.length < 100) {
    console.log('[Suggestions] Transcript too short, returning empty response')
    return {
      questions: [],
      proposals: []
    }
  }

  const result = await chatCompletion([
    {
      role: "system",
      content: SYSTEM_PROMPT,
    },
    {
      role: "user",
      content: userPrompt,
    },
  ], {
    temperature: 0.8,  // より創造的な回答を促す
    maxTokens: 4000,    // 思考プロセス + JSONに十分なトークン数
  })

  // デバッグ：LLMの生の出力をログに出力
  console.log("[LLM Raw Output Length]:", result.content?.length || 0)
  console.log("[LLM Raw Output Preview]:", result.content?.substring(0, 500) || "")

  // Chain of Thoughtの出力からJSON部分のみを抽出
  let content = result.content || "{}"

  // JSONコードブロックを抽出（```json と ``` の間）
  const jsonMatch = content.match(/```json\s*([\s\S]*?)\s*```/)

  if (jsonMatch && jsonMatch[1]) {
    // JSONコードブロックが見つかった場合
    const extractedJson = jsonMatch[1].trim()

    // デバッグ：抽出したJSONのプレビュー
    console.log("[Extracted JSON Preview]:", extractedJson.substring(0, 200))

    content = extractedJson
  } else {
    // フォールバック：Markdownコードブロックを削除
    console.warn("[WARN] JSON code block not found, falling back to markdown removal")
    content = content.replace(/```json\n?/g, '').replace(/```\n?/g, '').trim()
  }

  return JSON.parse(content)
}
